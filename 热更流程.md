# Unity3D 游戏热更新流程

```mermaid
flowchart TD
    A["🎮 游戏启动"] --> B["初始化基础模块<br/>(日志/配置/网络)"]
    B --> C{"检查网络连接"}
    C -->|无网络| D["提示网络异常<br/>可选：跳过更新直接进游戏"]
    C -->|有网络| E["请求服务器版本信息"]
    E --> F{"服务器可达？"}
    F -->|不可达| D
    F -->|可达| G["获取最新版本号 & 资源清单"]
    G --> H{"本地版本 < 服务器版本？"}
    H -->|否| I["无需更新<br/>直接进入游戏"]
    H -->|是| J["下载更新配置文件<br/>(filelist.json / manifest)"]
    J --> K["比对文件差异<br/>(MD5 / CRC32 / 版本号)"]
    K --> L["生成下载列表<br/>计算总下载大小"]
    L --> M["弹出更新提示<br/>显示大小 & 更新内容"]
    M --> N{"用户确认？"}
    N -->|取消| O{"是否强制更新？"}
    O -->|是| P["退出游戏"]
    O -->|否| I
    N -->|确认| Q["开始下载资源"]
    Q --> R["逐文件下载<br/>(支持断点续传)"]
    R --> S["边下边校验<br/>单个文件完整性"]
    S --> T{"全部下载完成？"}
    T -->|否| U{"下载失败？"}
    U -->|网络中断| V["保存进度<br/>下次启动续传"]
    U -->|重试| Q
    T -->|是| W["校验全部文件完整性"]
    W --> X{"校验通过？"}
    X -->|否| Y["标记损坏文件<br/>重新下载"]
    Y --> Q
    X -->|是| Z["应用热更资源<br/>(解压 / 覆盖 / 替换)"]
    Z --> AA["更新本地版本号"]
    AA --> AB["清理旧资源缓存"]
    AB --> I
    I --> AC["加载热更程序集<br/>(HybridCLR / ILRuntime / XLua)"]
    AC --> AD["加载热更资源<br/>(AssetBundle / Addressable)"]
    AD --> AE["进入游戏主场景"]
```

## 关键步骤说明

| 步骤 | 说明 |
|------|------|
| 版本比对 | 通过服务器版本号与本地版本号比较，判断是否需要更新 |
| 差异比对 | 逐文件比对 MD5 / CRC，只下载有变化的文件，减少流量 |
| 断点续传 | 下载中断后记录进度，下次启动从断点继续 |
| 完整性校验 | 下载完成后校验文件，确保未被篡改或损坏 |
| 资源应用 | 将下载的 AssetBundle / DLL / Lua 脚本覆盖到本地 |
| 热更程序集 | 通过 HybridCLR / ILRuntime / XLua 加载热更代码 |


注意：
 使用addressableSystem进行热更时，要想实现在游戏开始运行时执行下载更新过程，则需要注意
 - 1. 生成catalog文件（buildCatalog选项），在addressable的settiing文件里配置，
 - 2. 然后需要手动更新，不要addressable初始化时，就自动下载更新catalag了
